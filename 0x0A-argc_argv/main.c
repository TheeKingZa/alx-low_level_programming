#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include "0-whatsmyname.c"

/**
 * main - Entry point of the program
 * @argc: The number of command-line arguments
 * @argv: An array of command-line argument strings
 *
 * Return: 0 (Always success)
 */

int main(void)
{
_whatsmyname(argc, argv);

return (0);
}
