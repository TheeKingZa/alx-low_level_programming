include <stdio.h>
/**
 * Main - Puts funtion to print message.
 *
 * Return 0 always
 */
int main(void)
{
puts(""Programming is like building a multilingual puzzle");
return (0);
}

/* Returns 0 to indicate successful execution. */
